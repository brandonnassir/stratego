# Phase 7 Agent 6 — Final Acceptance and Library Freeze

## Role

You are **Agent 6**, the final implementation/validation agent in sequential Phase 7.

Work from the repository root.

You may recommend Phase 7 `PASS`, `FAIL`, or `BLOCKED`.

The reviewing chat makes formal acceptance.

Do not begin Phase 8.

---

## Prerequisite

Agents 1–5 must all report `PASS`.

Read every Phase 7 artifact and the complete Phase 7 implementation report.

At minimum:

```text
00_PHASE_7_SEQUENCE_AND_COMMON_CONTRACT.md
reports/phase_7_implementation_report.md
reports/phase_7_data/

data/setups/setup_library_v1.jsonl
data/setups/setup_library_v1_manifest.json

reports/phase_6_implementation_report.md
```

Verify the live frozen stack and all Phase 7 digests before doing any new validation.

Run the full repository suite before Agent 6 changes.

---

## Mission

Independently decide whether Phase 7 is ready to freeze.

You must:

1. reproduce the accepted library identities and counts;
2. regenerate the entire 8,000-base library from scratch;
3. compare regenerated bytes/content digests with the accepted materialized library;
4. review every Agent 1 diversity threshold and Agent 3 measurement;
5. review Agent 4 procedural stress and sampler determinism;
6. review Agent 5 collection/provenance/observer-safety integration;
7. choose one exact neutral Phase 8 setup-sampling profile using only structural coverage/diversity evidence;
8. emit the final Phase 7 manifest/decision.

Do not invent a new family or generator.

---

## Full-library regeneration

Regenerate all 8,000 bases into a fresh temporary location from only the recorded:

```text
contract versions
master seed
family ids
base indices / deterministic attempt identities
```

Do not read the production setup bytes as generator input.

Require:

```text
entry count                         exact
entry-by-entry semantic IDs         exact
entry-by-entry canonical setups     exact
entry-by-entry fingerprints         exact
entry-by-entry splits               exact
entry-by-entry traits               exact
library content digest              exact
metadata/manifest digest            exact where timestamp-free
```

If manifests contain timestamps, compare the deterministic content domain separately.

Hard gate:

```text
regeneration mismatches = 0
```

---

## Independent count/frozen-contract check

Confirm:

```text
8,000 bases
16 families
500/family

6,400 train
800 validation
800 test

400/50/50 per family
```

Confirm the live frozen stack still states:

```text
rules                  stratego_project_v1
reference engine       phase2_1_reference_1.2.0
observation            observation_v2_1_127ch
model contract         model_contract_v2
primary model          C1
fallback model         C0
backend                KEEP_PYTHON
trajectory             trajectory_v1
Phase 4 bank           evaluation_setup_bank_v1
```

No Phase 7 acceptance may silently advance any of those versions.

---

## Diversity acceptance review

For each Agent 1 threshold, build one final table:

```text
metric
scope/family
required threshold
Agent 3 measured value
margin
PASS/FAIL
```

Do not recompute a new threshold.

Do not average a failing family into a passing global mean.

All hard family-specific thresholds must pass.

Explicitly review:

```text
exact/reflection duplicates
cross-split equivalents
cross-split near-duplicate thresholds
within-family distance thresholds
Flag/Bomb/Scout/Miner positional coverage
high-rank placement coverage
structural trait diversity
```

Family overlap/confusion may be descriptive unless Agent 1 made a particular overlap threshold hard.

---

## Procedural sampler review

Verify Agent 4 completed >=100,000 stress outputs and that:

```text
engine-invalid                  0
inventory failures              0
stranded outputs                0
family violations               0
split leakage                   0
rebuild failures                0
serialization/reflection errors 0
```

Review effective-diversity evidence.

Do not require every perturbation output to be unique.

Do require that procedural sampling materially expands effective support rather than collapsing to a tiny repeated set.

---

## Final Phase 8 sampling profile

Choose and freeze one exact **neutral structural sampling profile** for Phase 8.

This decision may use only:

```text
family balance
base balance
reflection balance
procedural-diversity measurements
perturbation rejection rate
family-contract preservation
runtime overhead
```

Do not use:

```text
game outcomes
win rate
Elo
model value
policy score
human strength judgment
```

The profile must keep:

```text
split = train
family selection = uniform
base selection within family = uniform
reflection = 50/50 seeded
```

unless there is a compelling structural reason from accepted Phase 7 evidence to alter one of these, in which case stop and request review rather than silently changing the common contract.

Your main choice is the exact versioned **perturbation-use/intensity mixture** among Agent 4's accepted profiles.

Prefer a simple profile that meaningfully expands diversity without frequent rejection or family erosion.

Record the exact probabilities and version.

Do not tune them from game results.

---

## Integration acceptance review

Verify Agent 5's headline campaign:

```text
>=4,096 completed games
all 256 ordered family pairs represented
train split default
explicit validation/test access works
observer-safe boundary unchanged
Phase 4 bank unchanged
trajectory/provenance match
reconstruction clean
```

Spot-check/re-run a deterministic subset if needed.

No new multi-hour soak is required for Phase 7 unless Agent 5 uncovered a setup-specific stability risk.

Phase 6 already owns long-duration collection stability.

---

## Final library freeze

Expected final identities:

```text
setup_generator_contract_v1
setup_family_v1
setup_trait_vector_v1       if Agent 1 created it
setup_library_v1
setup_perturbation_v1
setup_sampler_v1
```

Use the actual accepted names from Agents 1–4.

Freeze:

```text
contract versions
family definitions
diversity thresholds
master seed
8,000 base entries
stable base IDs
split assignments
canonicalization/reflection semantics
library digest
perturbation semantics
sampler semantics
Phase 8 default train sampling profile
```

A later semantic change requires a new version.

Do not silently edit `setup_library_v1`.

---

## Final Phase 7 manifest

Create a compact final acceptance record containing at minimum:

```text
phase
status
commit
working_tree_state
frozen upstream versions

setup contract version
family version
trait version
library version
perturbation version
sampler version

master seed
base count
family counts
split counts

library digest
manifest digest
regeneration digest
regeneration mismatches

diversity gate summary
procedural stress summary
pipeline integration summary
observer-safety summary

default Phase 8 sampler profile

tests before
tests after
files created
files modified
completion gates
problems
limitations
```

---

## Suggested Agent 6 files

```text
scripts/run_phase7_agent06.py
tests/setups/test_phase7_final_acceptance.py
```

Do not duplicate the whole audit framework unless necessary.

---

## Required artifacts

Create:

```text
reports/phase_7_data/agent_06_final_acceptance.json
reports/phase_7_data/agent_06_library_regeneration.json
reports/phase_7_data/agent_06_sampler_profile.json
```

Append only:

```markdown
## 6. Agent 6 — Final Acceptance and Library Freeze
```

to:

```text
reports/phase_7_implementation_report.md
```

---

## Final tests

Run:

1. full suite before Agent 6 edits;
2. full library regeneration;
3. focused final acceptance tests;
4. any deterministic pipeline spot-check;
5. write all final artifacts;
6. full repository suite after artifacts exist.

Artifact-validation tests must execute rather than skip due to files being absent.

Record exact commands and durations.

---

## Stop conditions

Recommend `BLOCKED` if:

- any Agent 1–5 prerequisite is not PASS;
- accepted artifacts and live source disagree;
- full-library regeneration is not exact;
- a diversity threshold cannot be verified from available evidence;
- sampler/pipeline integration requires an upstream semantic change;
- the Phase 8 sampler profile cannot be chosen without outcome/strength evidence.

Recommend `FAIL` if the accepted Phase 7 implementation demonstrably violates its frozen contract and cannot be repaired within Agent 6 scope without redesign.

Do not weaken a gate to obtain PASS.

---

## PASS gates

Recommend Phase 7 `PASS` only if:

- Agents 1–5 all PASS;
- frozen upstream stack verified unchanged;
- full library regenerates exactly;
- 8,000/16/500 counts exact;
- 6,400/800/800 split exact;
- 0 invalid/stranded bases;
- 0 exact/reflection duplicates;
- 0 split-equivalent leakage;
- all Agent 1 diversity thresholds pass;
- >=100,000 procedural stress accepted with 0 hard errors;
- procedural support materially expands effective diversity;
- pipeline integration campaign passes;
- observer-safe model boundary unchanged;
- Phase 4 setup bank unchanged;
- one exact neutral Phase 8 sampler profile frozen;
- no game strength/outcome input used;
- no meaningful neural training occurred;
- full repository suite green.

---

## Final handoff

Return a concise final handoff containing:

```text
Phase 7 status

frozen setup contract version
frozen family version
frozen trait version
frozen library version
frozen perturbation version
frozen sampler version

master seed
library digest

base setups
families
family counts
train/validation/test counts

duplicate/reflection leakage
mobility failures
family failures
diversity threshold status

procedural stress count
procedural hard failures
procedural unique/diversity summary

pipeline integration games
family-pair coverage
trajectory/provenance mismatches
observer-safety violations

default Phase 8 sampler profile

tests before/after
commit / working-tree state

known limitations

Phase 8 recommendation:
READY TO PLAN
or
BLOCKED
```

Stop after the handoff.

Do not begin Phase 8 training.
