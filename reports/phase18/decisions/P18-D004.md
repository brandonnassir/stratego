# P18-D004 — Gate G2: parity holds, the learner learns, the gate's evaluation model lags far behind it

**Decision: `REVISE`.** Every parity requirement passes and the setup learner
demonstrably learns the frozen synthetic landscape on all three seeds, but the
frozen EMA-measured criterion was not met inside the 64-update budget. The EMA
at decay 0.999, updated once per setup update, retained `0.999^64 = 0.937975`
of the initial parameter contribution after 64 updates, which gives it an
approximately 1,000-update time constant; and the frozen 64-update assay
empirically showed severe EMA lag: the raw actor learned strongly while the EMA
captured only a small portion of the observed change (1.3–2.8% of the raw
displacement). The EMA-horizon concern and its interpretation were predeclared
in the contract before any frozen seed ran. *(Wording corrected 2026-09-02 by
Phase 18 Agent 5 under operator instruction 07; see "Corrections" at the end.
The packet as reviewed claimed the criterion was unreachable by arithmetic;
that claim is withdrawn. No result, identity or number changed.)*

```text
work package              phase18_setup_integrated_warmstart
agent                     phase_18_agent_4
gate                      G2 - setup implementation parity and synthetic learning
run                       G2-SYNTHETIC-ASSAY-2026-A
authorized by             P18-D003 accepted as PROCEED (57281f6a…/7d930357…),
                          review 1c234900…, instruction 06 dd390ba5…
G2_SOURCE_COMMIT          354a4cad55a88dca6dcb24a21cf79cecc130008f
source tree               0bbccb82b21f5988020c620e51d6929274897f63
verification commit       11b5558   launch manifest commit   6f5297e
result evidence commit    662362149f68c0c3125e656b24ab5d87dd93d253
```

The JSON packet is authoritative for identities and calculations. This file is
authoritative for the reading.

## The question

> Does the local scaled setup-policy implementation match the paper and the
> pinned published implementation at loss, gradient, sampling, aggregation,
> optimizer, checkpoint and EMA semantics — and can it reliably learn a known
> synthetic setup-reward landscape from outcome-only feedback across three
> fresh seeds?

Two inseparable sub-gates: method/implementation parity, and synthetic learning
with a known answer. `H0`: the EMA model's expected landscape utility does not
improve under the frozen criteria; `H1`: for every seed the final EMA utility
exceeds the initial, the pooled paired 95% lower bound is strictly above zero,
and the median seed closes at least 10% of its initial-to-optimum gap. The
decision reads the EMA after the final fixed update (64), never a best-of-run
checkpoint; the raw actor's held-out utility is recorded as a diagnostic that
decides nothing.

## What happened

**Parity.** All 30 method-map rows S01–S30 are implemented and complete from
recorded test outcomes; the independent float64 oracle agrees with the
production loss to 1.8e-15 and with production autograd to 2e-10 on thirteen
representative parameters; AdamW at zero decay, the clip scale, the EMA closed
form, the five-epoch step counts and the CPU/MPS checkpoint round trip all
match. 86/86 pre-existing Phase 18 evaluator tests and 122/122 new setup tests
pass.

**Assay.** Three frozen seeds, 64 updates each, 320 optimizer steps and 64 EMA
updates per seed, 1,021–1,024 ready setups per update with exactly four outcomes
each, 1,024 distinct setups per pool, reflection at 0.50, Flag in the permitted
half 100%. **Zero** legality, orientation, attribution, non-finite or
checkpoint-identity events. The landscape, the first period's pool and
outcomes, and both evaluation endpoints of every seed replay exactly from the
frozen seeds and the three-object checkpoints. No Stratego game played; sealed
Phase 8 access zero.

## The result

```text
                         seed 1        seed 2        seed 3
EMA initial -> final     -5.508 -> -5.339   -3.792 -> -3.484   -1.967 -> -1.766
EMA gap closed            +0.28%        +0.52%        +0.35%
raw initial -> final     -5.508 -> +7.281   -3.792 -> +7.201   -1.967 -> +6.567
raw gap closed           +20.92%       +18.50%       +14.82%

EMA pooled paired delta  +0.2261   95% [+0.1588, +0.2949]   n = 12,288
raw pooled paired delta +10.7718   95% [+10.6338, +10.9089]  n = 12,288
exact optimum 55.6234    uniform-random baseline -0.8136 (SD 6.1561)
EMA retained initial fraction  0.999^64 = 0.9380
```

Frozen EMA criteria: all three seeds improved — pass; pooled lower bound above
zero — pass; median gap closure ≥ 10% — **fail** (0.35%). The raw diagnostic
satisfies all three (median 18.50%). The EMA moved 1.3%, 2.8% and 2.4% of the
raw displacement, the few percent the decay arithmetic predicts.

## Separating the readings

**Observed fact.** Parity on every row and in the oracle. The raw actor's
held-out utility rises on all three seeds with tight, disjoint paired intervals;
its pool utility rises monotonically and is still rising at update 64; the
entropy head converges to `I/10` by update ~16 and the entropy-to-outcome
magnitude ratio falls from ~4 to ~0.2; prefix entropy holds at 1.6 nats; the
final-epoch reverse KL never exceeds 0.0012; the PPO clip never activates. The
EMA moves by a few percent of that and closes far less than 10% of the gap.

**Supported inference.** The implementation matches the published method at
every semantic the gate names, and it learns a known reward landscape from
outcome-only feedback reliably across three seeds. The gate's evaluation
model — the EMA at the paper's smoothing, with its approximately 1,000-update
time constant — lagged severely behind the raw actor within the 64-update
budget: it captured 1.3%, 2.8% and 2.4% of the observed raw displacement. The
frozen EMA criterion therefore had very little power at this budget; the
failing criterion reads as an instrument-horizon defect rather than a learning
failure, and that reading was predeclared before the run. This is an empirical
finding about this budget and this learner, not a proof that the EMA endpoint
was unreachable regardless of learner behaviour.

**Plausible but untested.** With a longer budget the EMA would eventually
follow the raw actor; the learning rate is set by the outcome term once the
entropy innovation is centered; the accelerating curve suggests more headroom.

**Claim not supported.** That the method learns *Stratego* setups (no game was
played); that the raw diagnostic satisfies the frozen gate (it was never the
decision model); any statement about a different budget, learning rate or
landscape.

## Sub-gates

| G2 sub-gate | status |
|---|---|
| S01–S30 parity rows complete from recorded tests | **pass** (30/30) |
| canned forward-loss and gradient parity | **pass** |
| zero legality / orientation / attribution / non-finite / checkpoint-identity failures | **pass** (0/0/0/0/0) |
| final EMA utility > initial EMA utility, all three seeds | **pass** |
| pooled paired 95% lower bound strictly > 0 (EMA) | **pass** (+0.1588) |
| median seed gap closure ≥ 10% (EMA) | **fail** (0.35%) |
| raw-actor diagnostic satisfies the same three criteria | pass — diagnostic, decides nothing |
| deterministic replay of landscape, outcomes and endpoints | **pass** (bitwise) |
| **Gate G2** | **REVISE** — parity passes; synthetic learning is shown on the raw actor; the EMA-based criterion was not met at this budget because the EMA (≈1,000-update time constant) lagged severely behind the raw actor — a predeclared instrument concern |

`STOP` is not the reading: a valid parity-correct implementation did not fail
to learn. `PROCEED` is not available: the frozen criteria are the instruction's
and one of them fails.

## Deviations

1. Two development smokes on separate namespaces (`phase18_g2_dev_smoke_v1`,
   `phase18_g2_dev_smoke_v2`; `reports/phase18/g2/dev_smoke_v1.json`) ran before
   the freeze. They informed two things: the raw-actor and pool-utility
   diagnostics that were added to the runner, and the predeclared
   instrument-defect interpretation of an EMA fail beside a raw pass. They did
   not change the frozen primary landscape, the budget, the threshold or the
   run seeds — all of which are the instruction's minima and the published
   values. The diagnostics decide nothing and the contract says so.
2. Two driver-only corrections between the first freeze commit and
   `G2_SOURCE_COMMIT` (oracle EMA tolerance; oracle `I − 10h` cross-check
   indexing); the frozen contract and landscape did not change.
3. `--verify` ran in the canonical checkout (it needs git-ignored accepted
   checkpoints); the launch manifest and all seeds ran from the clean worktree.
4. The three seeds ran concurrently; determinism is per-process and the replay
   reproduces both endpoints bitwise.
5. The replay script and the report are new files outside the launch manifest's
   digests; they change nothing the assay executed through.

## Proposed next question (one bounded correction)

> Under an amended G2 evaluation rule in which the synthetic assay's decision
> reads the raw generation actor's held-out utility — the EMA remaining the
> only evaluation model for every Stratego-facing stage, per S28 — do three
> fresh seeds of the same frozen landscape, budget and criteria pass?

This is the smallest change that gives the gate an instrument with power: it
keeps the landscape, budget, seeds' derivation, sample sizes, bootstrap and
threshold, and changes only which of the two already-recorded models the
criterion reads. The alternative that keeps the EMA — a budget long enough for
the EMA's ~1,000-update time constant — costs about 4 hours per seed at the
measured throughput and is the fallback if the reviewer prefers not to
distinguish the assay's evaluation model from the Stratego one. This run's raw
diagnostics already satisfy the criteria, so the reviewer may accept the
amendment on this evidence or require a fresh three-seed confirmation; either
way no Stratego game is involved and G3 remains unauthorized.

## Publication

**Published.** `P18-D004` was accepted as `REVISE` and the exact reviewed branch
HEAD was published by Phase 18 Agent 5 with a normal non-force push:

```text
remote        origin  https://github.com/brandonnassir/stratego.git
branch        phase18/g2-setup-parity
local SHA     6afa13bed355884a3327d2661fd739784260dc2b
remote SHA    6afa13bed355884a3327d2661fd739784260dc2b
published     2026-09-02T14:28:00Z
```

The remote branch was absent beforehand; no publication commit was added to
the reviewed branch. The corrected copy of this packet lives on
`phase18/g2-raw-confirmation`, created from that exact commit.

## Corrections (2026-09-02, Phase 18 Agent 5)

Applied under the operator's Agent 5 work package
(`instructions/phase_18_setup_integrated_warmstart/07_AGENT_5_G2_RAW_ACTOR_CONFIRMATION.md`)
after acceptance and publication. The decision, every identity and every number
in this packet are unchanged; only the interpretive wording listed here was
corrected. The packet as reviewed remains byte-identical at
`origin/phase18/g2-setup-parity` commit `6afa13bed355…` (Markdown
SHA-256 `d6e90e041e9f66bb…`, JSON SHA-256 `36fb4a3d9b8afd4a…`), and the
original wording of every corrected sentence is preserved in the JSON packet's
`corrections.items`.

1. **Withdrawn:** the claims that the EMA criterion "cannot be met inside the
   64-update budget by arithmetic" and "is unreachable at this budget", and
   that the evaluation model "cannot see" or "cannot reflect" the learning. The
   decay arithmetic gives the fraction of the initial parameters the EMA still
   carries and its time constant; it does not bound the EMA's held-out utility
   regardless of learner behaviour, because that utility is a nonlinear
   function of the parameters and depends on the raw trajectory.
2. **Stated:** decay 0.999 retained `0.999^64 = 0.937975` of the initial
   parameter contribution after 64 once-per-update blends, an approximately
   1,000-update time constant (`1 / (1 − 0.999)`).
3. **Stated:** the frozen 64-update assay empirically showed severe EMA lag —
   the raw actor learned strongly (20.9%, 18.5%, 14.8% of the gap) while the EMA
   captured only a small portion of the observed change (1.3%, 2.8%, 2.4% of
   the raw displacement; 0.28%, 0.52%, 0.35% of the gap).
4. **Stated:** the two development smokes informed the raw-actor diagnostic and
   the instrument-defect interpretation; they did not change the frozen primary
   landscape, budget, threshold or run seeds.
5. The frozen contract's predeclared statement, quoted verbatim in the JSON
   packet under `question.predeclared_instrument_finding.statement`, contains
   the phrase "whatever the raw learner does". The contract file is frozen and
   unedited (its digest is bound by the launch manifest and the binding
   ledger); that phrase is superseded by the reading above.
